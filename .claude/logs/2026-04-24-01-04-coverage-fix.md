# Coverage AutoFix 工作記錄
- 執行時間：2026-04-24 01:04（Taipei）
- PR：https://github.com/jeff377/bee-library/pull/22
- 處理檔案數：2

## 覆蓋率補強摘要
| 檔案 | 補強前覆蓋率 | 新增測試數 | 預估補強後覆蓋率 |
|------|------------|---------|--------------|
| src/Bee.Business/System/SystemBusinessObject.cs | 89.8% | 1 | ~98% |
| src/Bee.Business/System/SystemExecFuncHandler.cs | 35.3% | 2 | ~60%+ |

### SystemBusinessObject.cs
新增 `SaveDefine_LocalCallDbSchemaSettings_Succeeds`（`[Fact]`）：覆蓋
`SaveDefineCore` 私有方法的成功路徑，包含 `DefineFunc.GetDefineType`、
`SerializeFunc.XmlToObject`、`access.SaveDefine` 及 `return result` 等
原本完全未執行的分支（共約 7 行）。測試採 DbSchemaSettings 進行讀寫往返，
不修改現有資料。

### SystemExecFuncHandler.cs
- `ExecFunc_UpgradeTableSchema_ReturnsUpgradedStatus`（`[DbFact]`）：
  透過 `SystemBusinessObject.ExecFunc` 呼叫 `UpgradeTableSchema`，
  驗證既有 `st_user` 資料表的 schema 升級回傳含 `Upgraded` 參數。
- `ExecFunc_TestConnection_ValidDatabaseItem_Succeeds`（`[DbFact]`）：
  以 "common" 資料庫設定呼叫 `TestConnection`，驗證連線測試不拋出例外。

## 無法處理的檔案
| 檔案 | 原因 |
|------|------|
| src/Bee.Db/Providers/SqlServer/SqlTableSchemaProvider.cs | 34 行未覆蓋均在私有方法（ParsePrimaryKey/ParseIndexes/ParseDbField）內部分支，需特定 DB schema 組合（Decimal 欄位、Descending 索引、無 PK 資料表等）才能觸發；現有測試 DB 僅有 st_user/st_session，無法覆蓋這些邊界路徑 |
| src/Bee.Api.AspNetCore/Controllers/ApiServiceController.cs | 7 行未覆蓋集中於 HandleRequestAsync 的 catch(Exception) 區塊；JsonRpcExecutor.ExecuteAsyncCore 已自行吸收所有例外並轉為 JSON-RPC 錯誤回應，使 ApiServiceController 的 catch 在正常執行流程中無法觸達 |
| src/Bee.Definition/Security/MasterKeyProvider.cs | 6 行未覆蓋為 ReadAllTextShared 的 IOException 重試迴圈（attempt++ 路徑）與 LoadFromFile 中 FileMode.CreateNew 競爭條件的 catch(IOException)，需多執行緒同步協調才能可靠重現，不適合自動化補測 |
