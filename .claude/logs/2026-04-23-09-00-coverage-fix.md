# Coverage AutoFix 工作記錄
- 執行時間：2026-04-23 09:00（Taipei）
- PR：https://github.com/jeff377/bee-library/pull/20
- 處理檔案數：3

## 覆蓋率補強摘要
| 檔案 | 補強前覆蓋率 | 新增測試數 | 預估補強後覆蓋率 |
|------|------------|---------|--------------|
| `src/Bee.Business/System/SystemBusinessObject.cs` | 89.8% | 3 | ~97% |
| `src/Bee.Business/Provider/CacheDataSourceProvider.cs` | 50.0% | 1 | ~100% |
| `src/Bee.Repository/System/DatabaseRepository.cs` | 80.0% | 1 | ~90% |

### 補強說明

**SystemBusinessObject**（9 uncovered lines）
- `ExecFunc_UnknownMethod_ThrowsMissingMethodException`：呼叫 `ExecFunc("NoSuchMethodInHandler")` 觸發 `DoExecFunc` 委派路徑（`new SystemExecFuncHandler` + `BusinessFunc.InvokeExecFunc`）
- `SaveDefine_NonSensitiveType_EmptyXml_ThrowsInvalidOperation`：以空 XML 呼叫 `SaveDefine`（非敏感型別），觸發 `SaveDefineCore` 的 null 回傳路徑（`SerializeFunc.XmlToObject` 返回 null → `InvalidOperationException`）
- `SaveDefine_LocalCall_ValidXml_Succeeds`：先 GetDefine 取得 XML，再 SaveDefine 做 round-trip，覆蓋 `SaveDefineCore` 成功路徑

**CacheDataSourceProvider**（2 uncovered lines）
- `GetSessionUser_UnknownToken_ReturnsNull`（`[DbFact]`）：以未知 token 呼叫 `GetSessionUser`，覆蓋 `repo.GetSession(accessToken)` 回傳 null 路徑

**DatabaseRepository**（4 uncovered lines）
- `UpgradeTableSchema_ValidArgs_DoesNotThrow`（`[DbFact]`）：以有效參數 `"common"/"common"/"st_user"` 呼叫 `UpgradeTableSchema`，覆蓋 `TableSchemaBuilder` 建構與 `Execute` 呼叫路徑

## 無法處理的檔案
| 檔案 | 原因 |
|------|------|
| `src/Bee.Definition/Security/MasterKeyProvider.cs` | 剩餘 6 行位於 `LoadFromFile` 的 IOException race-condition catch（concurrent `FileMode.CreateNew` 失敗）與 `ReadAllTextShared` 的 transient IOException retry 迴圈，需模擬並行檔案鎖定才能觸發，無法在一般單元測試中覆蓋。 |
| `src/Bee.Base/Tracing/ITraceListener.cs` | 純介面宣告，無可執行程式碼，SonarCloud 標記的 2 行為介面定義行，無法透過測試覆蓋。 |
