# Coverage AutoFix 工作記錄
- 執行時間：2026-04-23 01:12（Taipei）
- PR：https://github.com/jeff377/bee-library/pull/19
- 處理檔案數：3

## 覆蓋率補強摘要
| 檔案 | 補強前覆蓋率 | 新增測試數 | 預估補強後覆蓋率 |
|------|------------|---------|--------------|
| `src/Bee.Db/DbAccess.cs` | 84.0% (39 uncovered) | 7 | ~92% |
| `src/Bee.Api.AspNetCore/Controllers/ApiServiceController.cs` | 53.0% (31 uncovered) | 7 | ~82% |
| `src/Bee.Repository/System/SessionRepository.cs` | 53.1% (23 uncovered) | 5 | ~88% |

### 補強內容說明

**DbAccess.cs**（`tests/Bee.Db.UnitTests/DbAccessTransactionTests.cs` 新增）
- `Execute(spec, transaction)` 搭配 Scalar 與 DataTable 種類（DbFact）
- `ExecuteAsync(spec)` 搭配 Scalar 種類，涵蓋未測試的 ExecuteAsync Scalar branch（DbFact）
- `ExecuteAsync(spec, transaction)` 搭配 Scalar 與 DataTable 種類（DbFact）
- `transaction.Connection == null` 保護例外路徑（同步/非同步各一，無需 DB）

**ApiServiceController.cs**（`tests/Bee.Api.AspNetCore.UnitTests/ApiServiceControllerTests.cs` 新增）
- 錯誤 Content-Type → 415
- 缺少 Content-Type → 415
- 空請求主體 → 400
- 無效 JSON → 400 ParseError
- 缺少 method 欄位 → 400
- 缺少 API 金鑰 → 401
- 需要認證但無 Authorization 標頭 → 401

**SessionRepository.cs**（`tests/Bee.Repository.UnitTests/SessionRepositoryTests.cs` 擴充）
- `GetSession` 不存在 token → null
- `GetSession` 有效 token → 回傳 SessionUser
- `GetSession` 已過期 Session → null（涵蓋 Delete 路徑）
- `CreateSession` 不存在使用者 → InvalidOperationException
- `GetSession` 一次性 Session → 取回後刪除（涵蓋 OneTime + Delete 路徑）

## 無法處理的檔案
| 檔案 | 原因 |
|------|------|
| `src/Bee.Db/Providers/SqlServer/SqlTableSchemaProvider.cs` | 剩餘未覆蓋行位於私有方法（ParsePrimaryKey、ParseIndexes、ParseDbField）內的特定分支，需要 DB 表格含有 AutoIncrement、Decimal 欄位及特定 index 組合，現有種子資料（st_user）無法涵蓋這些路徑 |
| `src/Bee.Business/System/SystemExecFuncHandler.cs` | `UpgradeTableSchema` 與 `TestConnection` 為 internal class 的靜態方法，需要完整初始化的 `RepositoryInfo.SystemProvider` 並透過 `ExecFunc` 呼叫（需認證）；複雜度超出自動補強範圍 |
