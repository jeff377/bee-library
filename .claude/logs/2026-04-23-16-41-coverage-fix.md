# Coverage AutoFix 工作記錄
- 執行時間：2026-04-23 16:41（Taipei）
- PR：https://github.com/jeff377/bee-library/pull/21
- 處理檔案數：2

## 覆蓋率補強摘要
| 檔案 | 補強前覆蓋率 | 新增測試數 | 預估補強後覆蓋率 |
|------|------------|---------|--------------|
| `src/Bee.Db/DbAccess.cs` | 85.6% | 5 | ~90%+ |
| `src/Bee.Business/System/SystemBusinessObject.cs` | 89.8% | 1 | ~93%+ |

### 補強內容說明

**DbAccess.cs → `DbAccessIsolationLevelTests.cs`（新增）**
- `ExecuteBatch_WithIsolationLevel_Succeeds`：覆蓋 `ExecuteBatch` 中 `IsolationLevel.HasValue = true` 分支
- `ExecuteBatchAsync_WithIsolationLevel_Succeeds`：覆蓋 `ExecuteBatchAsync` 中 `IsolationLevel.HasValue = true` 分支
- `ExecuteBatch_DataTableCommand_Succeeds`：覆蓋 `ExecuteBatch` 批次中 `DbCommandKind.DataTable` 分支
- `ExecuteBatchAsync_DataTableCommand_Succeeds`：覆蓋 `ExecuteBatchAsync` 批次中 `DbCommandKind.DataTable` 分支
- `UpdateDataTable_WithIsolationLevel_Succeeds`：覆蓋 `BeginTransactionIfRequested` 的 `IsolationLevel.HasValue = true` 分支

**SystemBusinessObject.cs → `SystemBusinessObjectExtraTests.cs`（修改）**
- `ExecFunc_Hello_AuthenticatedCall_ReturnsGreeting`：覆蓋 `DoExecFunc`（已驗證路徑），同時間接覆蓋 `SystemExecFuncHandler` 建構函式的 Authenticated 呼叫路徑

## 無法處理的檔案
| 檔案 | 原因 |
|------|------|
| `src/Bee.Db/Providers/SqlServer/SqlTableSchemaProvider.cs` | 34 個未覆蓋行均在私有 DB 方法（`ParseIndexes` 迴圈主體、`ParseDbField` 的 AutoIncrement/Decimal/NCHAR 分支），需要具備特定 schema（含非 PK 索引、Identity 欄位、Decimal 欄位）的 DB 資料表；在不知道確切測試 DB schema 的情況下無法安全補強 |
| `src/Bee.Api.AspNetCore/Controllers/ApiServiceController.cs` | 7 個未覆蓋行為 `HandleRequestAsync` 的 `catch` 區塊與 `IsDevelopment` 屬性；`JsonRpcExecutor.ExecuteAsyncCore` 已內建全面的例外處理（內層 try/catch），正常運作下 catch 區塊無法到達（實際死碼） |
| `src/Bee.Business/System/SystemExecFuncHandler.cs` | `UpgradeTableSchema` 與 `TestConnection` 方法皆依賴 `RepositoryInfo.SystemProvider!.DatabaseRepository`，需要具備特定 DB 資料表（`st_user` schema 等）才能執行；且類別為 `internal`，僅能透過 `SystemBusinessObject` 間接測試，DB 相關路徑已歸入 DB 測試範圍 |
