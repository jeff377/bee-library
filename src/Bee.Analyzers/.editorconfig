# Roslyn analyzer 慣例：`DiagnosticDescriptor` 一律以 PascalCase 的 `private static readonly`
# 宣告（`Rule`、`UnknownProgId` 等），Roslyn 自己的原始碼與所有官方樣板皆如此。
# 這些宣告與框架其餘部分的 `s_` 慣例衝突，但改成 `s_rule` 反而背離該領域的共同認知，
# 故在此資料夾關掉該條規則，而非在每個檔案加 pragma。
#
# WARNING: 這裡以**規則名稱**覆寫，所以根 `.editorconfig` 一改名，這道豁免就靜默失效
# （2026-09-04 踩過一次：`private_static_readonly_fields_s_prefix` 改名為
# `private_static_fields_s_prefix`，此處沒跟，整個組件炸出 38 個 IDE1006）。
# 改根檔的規則名時，這一行必須一起改。
[*.cs]
dotnet_naming_rule.private_static_fields_s_prefix.severity = none
