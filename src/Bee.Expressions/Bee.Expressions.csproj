<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <Description>Portable expression evaluation engine (DynamicExpresso-backed) shared by the business layer and UI clients for field computation and rule validation.</Description>
    <PackageTags>bee.net;expression;formula;rule;dynamicexpresso;enterprise</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="DynamicExpresso.Core" Version="2.19.3" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Bee.Base\Bee.Base.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Bee.Expressions.UnitTests" />
  </ItemGroup>

</Project>
