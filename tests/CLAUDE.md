# 巢狀 CLAUDE.md 載入時機探針（暫時檔，驗完即刪或改寫）

PROBE-TESTS-SCOPE-LOADED-9f3a1c

## 為什麼有這個檔

`.claude/rules/` 有 77% 的內容是**路徑限定**的（`testing.md` 只在寫測試時需要、
`avalonia.md` 只在改控件時需要…），但它們全部經 `@import` 常駐，每個 session 都付成本。

若巢狀 `CLAUDE.md` 是「觸及該目錄才載入」，就能把 `testing.md`（最大的常駐檔）下沉到這裡，
省下約 17,000 字元。**但前提是它真的會被載入** —— 否則規則會靜默消失，不會有任何錯誤訊息。

## 怎麼驗（開一個新 session，問兩次）

1. **session 一開始就問**：「你的 context 裡有 `PROBE-TESTS-SCOPE-LOADED-9f3a1c` 嗎？」
2. **讓它讀一個 `tests/` 下的檔案**（例如 `tests/Bee.Tests.Shared/BeeTestFixture.cs`），
   再問同一句。

## 判讀

| 第 1 次 | 第 2 次 | 結論 | 該怎麼做 |
|---------|---------|------|---------|
| 有 | 有 | **啟動即載**，不是 lazy | 放棄此路，省不到成本 |
| 沒有 | **有** | **觸及才載** ✅ | 把 `testing.md` 下沉到本檔，常駐只留指路 |
| 沒有 | 沒有 | **巢狀 CLAUDE.md 不生效**（只認 root） | 放棄此路，改走 `PreToolUse` hook |

第三種是最危險的情況：規則搬過來會**靜默失效**。所以必須先驗，不能先搬。

> 本 session（2026-08-12）內測不出來 —— 探針檔是 session 啟動**之後**才建立的，
> 「啟動即載」與「不生效」兩種情況在這裡的觀測結果完全相同（都不會注入）。
